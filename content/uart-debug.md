# UART 通讯调试基础

UART 调试最容易犯的错误，是还没确认物理层就开始分析协议。

## 1. 先确认物理层

- TTL 电平是 3.3 V 还是 5 V。
- TX、RX、GND 是否正确。
- 单线通讯是否需要特殊收发结构。
- 是否共地。

## 2. 再确认串口参数

常见参数：波特率、数据位、停止位、校验位。

## 3. 抓真实报文

先从真实设备抓一组稳定报文，再分析帧头、长度、命令、数据区与校验。

```text
真实设备 -> 抓包 -> 解析 -> 验证 -> 模拟
```

这个顺序通常比直接猜协议更高效。
